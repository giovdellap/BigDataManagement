import { Routes } from '@angular/router';
import { LibraryTestComponent } from './components/library-test/library-test.component';

export const routes: Routes = [
  {path: "", component: LibraryTestComponent}
];
